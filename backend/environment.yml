name: cs224G_techpack_ai
channels:
  - conda-forge  
  - defaults
dependencies:
  - python=3.10  
  - pip
  - matplotlib
  - pip:
      - torch
      - torchvision
      - openai
      - flask
      - supabase
      - flask_cors
      - loguru
      - werkzeug
      - transformers
      - opencv-python
